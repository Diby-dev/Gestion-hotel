import { useState } from 'react';

const initialRooms = [
  { name: 'Chambre Standard 101', status: 'Occupée', price: '20 000 FCFA', guest: 'Jean Kouassi' },
  { name: 'Chambre Deluxe 102', status: 'Disponible', price: '28 000 FCFA', guest: '—' },
  { name: 'Suite Prestige 201', status: 'Réservée', price: '38 000 FCFA', guest: 'Aminata Traoré' },
];

const initialRates = { night: 25000, 1: 1000, 2: 2000, 3: 4000, 4: 5000 };
const formatPrice = (value) => `${Number(value || 0).toLocaleString('fr-FR')} FCFA`;

function StatusBadge({ status }) {
  const styles = status === 'Disponible' ? 'bg-emerald-100 text-emerald-800' : status === 'Occupée' ? 'bg-orange-100 text-orange-800' : 'bg-amber-100 text-amber-800';
  return <span className={`rounded-full px-3 py-1 text-xs font-bold ${styles}`}>{status}</span>;
}

export default function OwnerDashboard() {
  const [active, setActive] = useState('Aperçu');
  const [rates, setRates] = useState(initialRates);
  const [rooms, setRooms] = useState(initialRooms);
  const [photos, setPhotos] = useState(['/hotel-yop2.jpg']);
  const [saved, setSaved] = useState(false);
  const [boosted, setBoosted] = useState(false);
  const [isRoomFormOpen, setIsRoomFormOpen] = useState(false);
  const [roomForm, setRoomForm] = useState({ number: '', type: 'Chambre Standard', nightPrice: 20000, hourlyPrice: 1000, capacity: 2 });
  const [isEditingEstablishment, setIsEditingEstablishment] = useState(false);
  const [establishmentSaved, setEstablishmentSaved] = useState(false);
  const [establishment, setEstablishment] = useState({ name: 'Palais de Niangon', commune: 'Yopougon', address: 'Niangon Sud à droite, Carrefour Lubafrique, Abidjan', phone: '+225 07 07 07 07 07', email: 'contact@palaisniangon.ci', description: 'Un cadre calme, spacieux et sécurisé pour vos séjours à Abidjan.' });
  const menuItems = ['Aperçu', 'Mes chambres', 'Tarifs', 'Booster mon hôtel', 'Réservations', 'Mon établissement'];

  const updateRate = (key, value) => {
    setRates((current) => ({ ...current, [key]: Math.max(0, Number(value)) }));
    setSaved(false);
  };

  const addPhotos = (event) => {
    const files = Array.from(event.target.files || []);
    files.forEach((file) => {
      const reader = new FileReader();
      reader.onload = () => setPhotos((current) => [...current, reader.result]);
      reader.readAsDataURL(file);
    });
    event.target.value = '';
  };

  const saveChanges = () => {
    setSaved(true);
    window.setTimeout(() => setSaved(false), 3500);
  };

  const addRoom = (event) => {
    event.preventDefault();
    setRooms((current) => [...current, { name: `${roomForm.type} ${roomForm.number}`, status: 'Disponible', price: formatPrice(roomForm.nightPrice), hourlyPrice: formatPrice(roomForm.hourlyPrice), guest: '—' }]);
    setRoomForm({ number: '', type: 'Chambre Standard', nightPrice: 20000, hourlyPrice: 1000, capacity: 2 });
    setIsRoomFormOpen(false);
  };

  const saveEstablishment = (event) => {
    event.preventDefault();
    setIsEditingEstablishment(false);
    setEstablishmentSaved(true);
    window.setTimeout(() => setEstablishmentSaved(false), 3500);
  };
  const RatesPanel = () => <section className="rounded-3xl bg-white p-6 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-xs font-black uppercase tracking-[.18em] text-orange-600">Réservations & prix</p><h2 className="mt-1 text-2xl font-black text-slate-950">Tarifs de votre établissement</h2><p className="mt-1 text-sm text-slate-500">Modifiez les prix proposés aux clients, de la nuit au court séjour.</p></div><button type="button" onClick={saveChanges} className="rounded-xl bg-emerald-700 px-5 py-3 text-sm font-bold text-white shadow-sm transition hover:bg-emerald-800">Enregistrer les tarifs</button></div>{saved && <p className="mt-4 rounded-xl bg-emerald-50 px-4 py-3 text-sm font-bold text-emerald-800">✓ Tarifs enregistrés dans cette démonstration.</p>}<div className="mt-6 grid gap-4 lg:grid-cols-5"><label className="rounded-2xl border-2 border-emerald-100 bg-emerald-50 p-4 lg:col-span-1"><span className="text-xs font-black uppercase tracking-wider text-emerald-800">Séjour nuit</span><span className="mt-2 block text-sm font-bold text-slate-700">Prix de départ / nuit</span><input type="number" min="0" value={rates.night} onChange={(event) => updateRate('night', event.target.value)} className="mt-3 w-full rounded-xl border border-emerald-200 bg-white px-3 py-2 font-black text-emerald-800 outline-none focus:ring-2 focus:ring-emerald-400" /><span className="mt-2 block text-xs text-slate-500">{formatPrice(rates.night)}</span></label><div className="grid gap-3 sm:grid-cols-2 lg:col-span-4 xl:grid-cols-4">{[1, 2, 3, 4].map((hours) => <label key={hours} className="rounded-2xl border border-orange-100 bg-orange-50 p-4"><span className="text-xs font-black uppercase tracking-wider text-orange-700">Court séjour</span><span className="mt-2 block font-bold text-slate-800">{hours} heure{hours > 1 ? 's' : ''}</span><input type="number" min="0" value={rates[hours]} onChange={(event) => updateRate(hours, event.target.value)} className="mt-3 w-full rounded-xl border border-orange-200 bg-white px-3 py-2 font-black text-orange-800 outline-none focus:ring-2 focus:ring-orange-400" /><span className="mt-2 block text-xs text-slate-500">{formatPrice(rates[hours])}</span></label>)}</div></div><div className="mt-6 rounded-2xl bg-slate-950 p-5 text-white"><p className="text-sm font-bold text-orange-300">Aperçu côté client</p><div className="mt-3 flex flex-wrap gap-2"><span className="rounded-full bg-white/10 px-3 py-2 text-sm">La nuit · {formatPrice(rates.night)}</span>{[1, 2, 3, 4].map((hours) => <span key={hours} className="rounded-full bg-white/10 px-3 py-2 text-sm">{hours} h · {formatPrice(rates[hours])}</span>)}</div></div></section>;

  const PhotosPanel = () => <section className="rounded-3xl bg-white p-6 shadow-sm"><div><p className="text-xs font-black uppercase tracking-[.18em] text-orange-600">Vitrine de votre hôtel</p><h2 className="mt-1 text-2xl font-black text-slate-950">Photos de l'établissement</h2><p className="mt-1 text-sm text-slate-500">Ajoutez des photos depuis votre galerie ou prenez une photo directement avec votre téléphone.</p></div><div className="mt-6 grid gap-4 sm:grid-cols-2">{photos.map((photo, index) => <div key={`${photo}-${index}`} className="group relative overflow-hidden rounded-2xl bg-slate-100"><img src={photo} alt={`Photo de l'hôtel ${index + 1}`} className="h-48 w-full object-cover" /><button type="button" onClick={() => setPhotos((current) => current.filter((_, photoIndex) => photoIndex !== index))} className="absolute right-3 top-3 rounded-lg bg-slate-950/80 px-3 py-2 text-xs font-bold text-white opacity-0 transition group-hover:opacity-100">Retirer</button></div>)}</div><div className="mt-6 grid gap-3 sm:grid-cols-2"><label className="cursor-pointer rounded-2xl border-2 border-dashed border-emerald-300 bg-emerald-50 p-5 text-center transition hover:bg-emerald-100"><input type="file" accept="image/*" multiple onChange={addPhotos} className="sr-only" /><span className="text-2xl">🖼️</span><span className="mt-2 block font-black text-emerald-900">Choisir des photos</span><span className="mt-1 block text-xs text-emerald-700">Depuis la galerie ou les fichiers</span></label><label className="cursor-pointer rounded-2xl border-2 border-dashed border-orange-300 bg-orange-50 p-5 text-center transition hover:bg-orange-100"><input type="file" accept="image/*" capture="environment" onChange={addPhotos} className="sr-only" /><span className="text-2xl">📷</span><span className="mt-2 block font-black text-orange-900">Prendre une photo</span><span className="mt-1 block text-xs text-orange-700">Ouvre la caméra sur téléphone</span></label></div><p className="mt-4 text-xs text-slate-500">Les photos sont visibles immédiatement dans cette version front-end. Elles seront sauvegardées définitivement quand le backend sera connecté.</p></section>;

  const EstablishmentPanel = () => <div className="mt-7 space-y-6"><section className="rounded-3xl bg-white p-6 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-xs font-black uppercase tracking-[.18em] text-orange-600">Fiche établissement</p><h2 className="mt-1 text-2xl font-black text-slate-950">{establishment.name}</h2><p className="mt-1 text-sm text-slate-500">Les informations visibles pour vos futurs clients.</p></div><button type="button" onClick={() => setIsEditingEstablishment((current) => !current)} className="rounded-xl bg-emerald-700 px-5 py-3 text-sm font-bold text-white transition hover:bg-emerald-800">{isEditingEstablishment ? 'Annuler' : 'Modifier l’établissement'}</button></div>{establishmentSaved && <p className="mt-4 rounded-xl bg-emerald-50 px-4 py-3 text-sm font-bold text-emerald-800">✓ Informations enregistrées dans cette démonstration.</p>}{isEditingEstablishment ? <form onSubmit={saveEstablishment} className="mt-6 rounded-2xl border border-emerald-100 bg-emerald-50 p-5"><div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-bold text-slate-700">Nom de l’établissement<input required value={establishment.name} onChange={(event) => setEstablishment({ ...establishment, name: event.target.value })} className="mt-1 w-full rounded-xl border border-slate-200 bg-white px-3 py-2.5 outline-none focus:ring-2 focus:ring-emerald-400" /></label><label className="text-sm font-bold text-slate-700">Commune<input required value={establishment.commune} onChange={(event) => setEstablishment({ ...establishment, commune: event.target.value })} className="mt-1 w-full rounded-xl border border-slate-200 bg-white px-3 py-2.5 outline-none focus:ring-2 focus:ring-emerald-400" /></label><label className="text-sm font-bold text-slate-700 sm:col-span-2">Adresse<input required value={establishment.address} onChange={(event) => setEstablishment({ ...establishment, address: event.target.value })} className="mt-1 w-full rounded-xl border border-slate-200 bg-white px-3 py-2.5 outline-none focus:ring-2 focus:ring-emerald-400" /></label><label className="text-sm font-bold text-slate-700">Téléphone<input required value={establishment.phone} onChange={(event) => setEstablishment({ ...establishment, phone: event.target.value })} className="mt-1 w-full rounded-xl border border-slate-200 bg-white px-3 py-2.5 outline-none focus:ring-2 focus:ring-emerald-400" /></label><label className="text-sm font-bold text-slate-700">E-mail<input required type="email" value={establishment.email} onChange={(event) => setEstablishment({ ...establishment, email: event.target.value })} className="mt-1 w-full rounded-xl border border-slate-200 bg-white px-3 py-2.5 outline-none focus:ring-2 focus:ring-emerald-400" /></label><label className="text-sm font-bold text-slate-700 sm:col-span-2">Présentation<textarea required rows="4" value={establishment.description} onChange={(event) => setEstablishment({ ...establishment, description: event.target.value })} className="mt-1 w-full resize-none rounded-xl border border-slate-200 bg-white px-3 py-2.5 outline-none focus:ring-2 focus:ring-emerald-400" /></label></div><button className="mt-5 rounded-xl bg-orange-500 px-5 py-3 text-sm font-black text-white transition hover:bg-orange-600">Enregistrer les informations</button></form> : <div className="mt-6 grid gap-4 sm:grid-cols-2"><div className="rounded-2xl bg-[#f7f3ea] p-4"><p className="text-xs font-black uppercase tracking-wider text-slate-500">Localisation</p><p className="mt-2 font-bold text-slate-900">{establishment.commune}</p><p className="mt-1 text-sm text-slate-600">{establishment.address}</p></div><div className="rounded-2xl bg-[#f7f3ea] p-4"><p className="text-xs font-black uppercase tracking-wider text-slate-500">Contact</p><p className="mt-2 font-bold text-slate-900">{establishment.phone}</p><p className="mt-1 text-sm text-slate-600">{establishment.email}</p></div><div className="rounded-2xl bg-[#f7f3ea] p-4 sm:col-span-2"><p className="text-xs font-black uppercase tracking-wider text-slate-500">Présentation</p><p className="mt-2 text-sm leading-6 text-slate-700">{establishment.description}</p></div></div>}</section><PhotosPanel /></div>;
  const Overview = () => <><div className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[['12', 'Chambres au total', 'text-slate-950'], ['8', 'Occupées ce soir', 'text-emerald-700'], ['3', 'Arrivées prévues', 'text-orange-600'], ['540 000', 'Revenus ce mois (FCFA)', 'text-slate-950']].map(([value, label, color]) => <article key={label} className="rounded-2xl bg-white p-5 shadow-sm"><p className={`text-2xl font-black ${color}`}>{value}</p><p className="mt-1 text-sm text-slate-500">{label}</p></article>)}</div><div className="mt-6 grid gap-6 xl:grid-cols-5"><article className="rounded-3xl bg-white p-6 shadow-sm xl:col-span-3"><div className="flex items-center justify-between"><div><p className="font-black text-slate-950">Occupation de la semaine</p><p className="text-xs text-slate-500">Taux moyen : 72 %</p></div><span className="rounded-full bg-emerald-100 px-3 py-1 text-xs font-bold text-emerald-700">+8 %</span></div><div className="mt-8 flex h-36 items-end justify-between gap-2">{[52, 68, 46, 75, 62, 88, 72].map((height, index) => <div key={index} className="flex flex-1 flex-col items-center gap-2"><div className={`w-full rounded-t-lg ${index === 5 ? 'bg-orange-500' : 'bg-emerald-600'}`} style={{ height: `${height}%` }} /><span className="text-[10px] text-slate-400">{['Lun', 'Mar', 'Mer', 'Jeu', 'Ven', 'Sam', 'Dim'][index]}</span></div>)}</div></article><article className="rounded-3xl bg-slate-950 p-6 text-white xl:col-span-2"><p className="text-sm font-bold text-orange-300">PROCHAINE ARRIVÉE</p><h3 className="mt-4 text-xl font-black">Aminata Traoré</h3><p className="mt-1 text-sm text-slate-300">Suite Prestige 201 · 16:00</p><div className="mt-6 border-t border-white/10 pt-4 text-sm"><p>2 nuits · 76 000 FCFA</p><p className="mt-2 text-orange-200">Paiement Mobile Money confirmé</p></div></article></div></>;

  const RoomsPanel = () => <section className="mt-7 overflow-hidden rounded-3xl bg-white shadow-sm"><div className="flex flex-wrap items-center justify-between gap-3 p-6"><div><h2 className="font-black text-slate-950">Inventaire hébergement</h2><p className="text-sm text-slate-500">État de vos chambres aujourd'hui.</p></div><button type="button" onClick={() => setIsRoomFormOpen((current) => !current)} className="rounded-xl bg-emerald-700 px-4 py-2.5 text-sm font-bold text-white transition hover:bg-emerald-800">{isRoomFormOpen ? 'Fermer le formulaire' : '+ Ajouter une chambre'}</button></div>{isRoomFormOpen && <form onSubmit={addRoom} className="mx-6 mb-6 rounded-2xl border border-emerald-100 bg-emerald-50 p-5"><h3 className="font-black text-slate-950">Nouvel hébergement</h3><p className="mt-1 text-sm text-slate-600">Ajoutez une chambre qui sera affichée dans votre inventaire.</p><div className="mt-4 grid gap-3 sm:grid-cols-2 xl:grid-cols-3"><label className="text-sm font-bold text-slate-700">Numéro de chambre<input required value={roomForm.number} onChange={(event) => setRoomForm({ ...roomForm, number: event.target.value })} placeholder="Ex. 203" className="mt-1 w-full rounded-xl border border-slate-200 bg-white px-3 py-2.5 outline-none focus:ring-2 focus:ring-emerald-400" /></label><label className="text-sm font-bold text-slate-700">Type<select value={roomForm.type} onChange={(event) => setRoomForm({ ...roomForm, type: event.target.value })} className="mt-1 w-full rounded-xl border border-slate-200 bg-white px-3 py-2.5 outline-none focus:ring-2 focus:ring-emerald-400"><option>Chambre Standard</option><option>Chambre Deluxe</option><option>Suite Prestige</option><option>Studio meublé</option></select></label><label className="text-sm font-bold text-slate-700">Capacité<select value={roomForm.capacity} onChange={(event) => setRoomForm({ ...roomForm, capacity: Number(event.target.value) })} className="mt-1 w-full rounded-xl border border-slate-200 bg-white px-3 py-2.5 outline-none focus:ring-2 focus:ring-emerald-400"><option value="1">1 personne</option><option value="2">2 personnes</option><option value="3">3 personnes</option><option value="4">4 personnes</option></select></label><label className="text-sm font-bold text-slate-700">Prix par nuit (FCFA)<input required type="number" min="0" value={roomForm.nightPrice} onChange={(event) => setRoomForm({ ...roomForm, nightPrice: Number(event.target.value) })} className="mt-1 w-full rounded-xl border border-slate-200 bg-white px-3 py-2.5 outline-none focus:ring-2 focus:ring-emerald-400" /></label><label className="text-sm font-bold text-slate-700">Prix par heure (FCFA)<input required type="number" min="0" value={roomForm.hourlyPrice} onChange={(event) => setRoomForm({ ...roomForm, hourlyPrice: Number(event.target.value) })} className="mt-1 w-full rounded-xl border border-slate-200 bg-white px-3 py-2.5 outline-none focus:ring-2 focus:ring-emerald-400" /></label></div><button className="mt-5 rounded-xl bg-orange-500 px-5 py-3 text-sm font-black text-white transition hover:bg-orange-600">Ajouter à l'inventaire</button></form>}<div className="divide-y divide-slate-100">{rooms.map((room) => <div key={room.name} className="flex flex-wrap items-center justify-between gap-3 p-5 text-sm"><div><p className="font-bold text-slate-950">{room.name}</p><p className="text-slate-500">{room.guest}</p></div><div className="text-right"><p className="font-bold">{room.price} / nuit</p>{room.hourlyPrice && <p className="mt-1 text-xs font-bold text-orange-700">{room.hourlyPrice} / heure</p>}</div><StatusBadge status={room.status} /></div>)}</div></section>;

  const BoostPanel = () => <section className="mt-7 overflow-hidden rounded-3xl bg-slate-950 text-white shadow-xl"><div className="bg-gradient-to-r from-emerald-800 to-orange-500 p-7"><p className="text-xs font-black uppercase tracking-[.18em] text-orange-100">Visibilité premium</p><h2 className="mt-2 text-3xl font-black">Booster mon hôtel</h2><p className="mt-2 max-w-xl text-sm text-emerald-50">Placez votre établissement en vedette auprès des clients qui recherchent un hôtel dans votre commune.</p></div><div className="grid gap-6 p-7 lg:grid-cols-[1fr_auto]"><div><div className="flex items-center gap-3"><span className="flex h-12 w-12 items-center justify-center rounded-2xl bg-orange-500 text-2xl">★</span><div><p className="font-black">Mise en vedette pendant 7 jours</p><p className="text-sm text-slate-300">Apparition prioritaire dans les résultats de recherche.</p></div></div><ul className="mt-6 space-y-3 text-sm text-slate-200"><li>✓ Badge « En vedette » sur votre hôtel</li><li>✓ Meilleure visibilité à Yopougon et dans les résultats</li><li>✓ Suivi de votre campagne dans cet espace</li></ul></div><div className="rounded-2xl bg-white p-6 text-slate-950 lg:w-72"><p className="text-xs font-black uppercase tracking-wider text-orange-600">Tarif unique</p><p className="mt-2 text-3xl font-black">2 000 FCFA</p><p className="mt-1 text-sm text-slate-500">Paiement Mobile Money</p><button type="button" onClick={() => setBoosted(true)} disabled={boosted} className="mt-5 w-full rounded-xl bg-emerald-700 py-3 text-sm font-black text-white disabled:bg-emerald-200">{boosted ? 'Campagne active ✓' : 'Payer et booster'}</button></div></div>{boosted && <div className="border-t border-white/10 bg-emerald-950 px-7 py-4 text-sm font-bold text-emerald-100">Votre hôtel est maintenant en vedette. La connexion à Mobile Money sera ajoutée avec le backend.</div>}</section>;
  const page = active === 'Tarifs' ? <RatesPanel /> : active === 'Booster mon hôtel' ? <BoostPanel /> : active === 'Mon établissement' ? <EstablishmentPanel /> : active === 'Mes chambres' ? <RoomsPanel /> : active === 'Réservations' ? <section className="mt-7 rounded-3xl bg-white p-6 shadow-sm"><h2 className="text-2xl font-black text-slate-950">Réservations</h2><p className="mt-2 text-slate-500">Les prochaines réservations apparaîtront ici.</p></section> : <Overview />;

  return <main className="min-h-screen bg-[#f7f3ea] px-4 pb-16 pt-28 sm:pt-32"><div className="mx-auto grid max-w-7xl gap-6 lg:grid-cols-[250px_1fr]"><aside className="rounded-3xl bg-slate-950 p-5 text-white shadow-xl"><div className="border-b border-white/10 pb-5"><p className="text-xs font-black uppercase tracking-[.18em] text-orange-300">Espace propriétaire</p><h1 className="mt-2 text-2xl font-black">GrandH Pro</h1><p className="mt-1 text-xs text-slate-400">L'hôtellerie ivoirienne, bien gérée.</p></div><nav className="mt-5 space-y-2">{menuItems.map((item) => <button key={item} type="button" onClick={() => setActive(item)} className={`w-full rounded-xl px-4 py-3 text-left text-sm font-bold transition ${active === item ? 'bg-orange-500 text-white shadow-lg shadow-orange-950/20' : 'text-slate-300 hover:bg-white/10'}`}>{item}</button>)}</nav><div className="mt-8 rounded-2xl bg-white/10 p-4 text-xs text-slate-300"><p className="font-bold text-white">Besoin d'aide ?</p><p className="mt-1">Votre conseiller GrandH est disponible 24h/24 à Abidjan.</p></div></aside><section className="min-w-0"><header className="overflow-hidden rounded-3xl bg-gradient-to-br from-emerald-800 via-emerald-700 to-orange-500 p-7 text-white shadow-lg"><p className="text-xs font-black uppercase tracking-[.2em] text-orange-100">Palais de Niangon · Yopougon</p><div className="mt-3 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><h2 className="text-3xl font-black">{active === 'Aperçu' ? 'Bonjour, M. Niang 👋' : active}</h2><p className="mt-1 text-sm text-emerald-50">Pilotez votre établissement et vos réservations avec sérénité.</p></div><span className="w-fit rounded-full bg-white/15 px-4 py-2 text-sm font-bold">● Établissement vérifié</span></div></header>{page}</section></div></main>;
}



