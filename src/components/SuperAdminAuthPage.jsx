import { useState } from 'react';
import { useModal } from '../context/ModalContext';

export default function SuperAdminAuthPage() {
  const { navigate } = useModal();
  const [mode, setMode] = useState('login');
  return <main className="min-h-[700px] bg-slate-950 px-4 pb-16 pt-32"><section className="mx-auto max-w-md overflow-hidden rounded-3xl bg-white shadow-2xl"><div className="bg-linear-to-br from-violet-700 to-slate-950 p-8 text-white"><p className="text-xs font-bold uppercase tracking-[.2em] text-violet-200">Accès restreint</p><h1 className="mt-3 text-3xl font-black">Super-admin</h1><p className="mt-2 text-sm text-violet-100">Environnement de démonstration GrandH.</p></div><div className="p-7"><div className="grid grid-cols-2 rounded-xl bg-slate-100 p-1"><button onClick={() => setMode('login')} className={`rounded-lg py-2.5 text-sm font-bold ${mode === 'login' ? 'bg-white shadow text-violet-700' : 'text-slate-500'}`}>Connexion</button><button onClick={() => setMode('register')} className={`rounded-lg py-2.5 text-sm font-bold ${mode === 'register' ? 'bg-white shadow text-violet-700' : 'text-slate-500'}`}>Enregistrer</button></div><form onSubmit={(e) => { e.preventDefault(); navigate('/super-admin/tableau-de-bord'); }} className="mt-6 space-y-4">{mode === 'register' && <label className="block text-sm font-bold">Nom administrateur<input required className="mt-2 w-full rounded-xl border border-slate-200 p-3 outline-none focus:ring-4 focus:ring-violet-100" /></label>}<label className="block text-sm font-bold">Identifiant<input required className="mt-2 w-full rounded-xl border border-slate-200 p-3 outline-none focus:ring-4 focus:ring-violet-100" placeholder="admin@grandh.ci" /></label><label className="block text-sm font-bold">Mot de passe<input required type="password" className="mt-2 w-full rounded-xl border border-slate-200 p-3 outline-none focus:ring-4 focus:ring-violet-100" /></label><button className="w-full rounded-xl bg-violet-700 py-3.5 font-bold text-white hover:bg-violet-800">{mode === 'login' ? 'Accéder au tableau de bord' : 'Créer l’accès admin'}</button></form><p className="mt-5 text-center text-xs text-slate-400">Simulation uniquement — aucune donnée réelle n'est enregistrée.</p></div></section></main>;
}
